<?xml version="1.0" encoding="UTF-8"?>
<project
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>\${groupId}</groupId>
        <artifactId>\${aidLower}-parent</artifactId>
        <version>\${version}</version>
    </parent>
    #set ($nameCaps = $artifactId.substring(0, 1).toUpperCase() + $artifactId.substring(1))

    <artifactId>\${aidLower}-generated-business-logic-spi</artifactId>

    <name>$nameCaps Generated Business Logic SPI</name>
    <description>Generated-code only - contains the generated interfaces for implementing
        a $nameCaps server.  Simply implement them and bind them using the generated
        \${nameCaps}Service Guice module in the \${aidLower}-generated-acteur-server or
        \${aidLower}-generated-vertx-server project.
    </description>

    <dependencies>
        <dependency>
            <groupId>\${project.groupId}</groupId>
            <artifactId>\${aidLower}-model</artifactId>
        </dependency>
        <dependency>
            <!-- Needed for the @ImplementedBy annotation -->
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    <!-- POM originally generated by the ${project.groupId}:${project.artifactId} Maven Plugin
    version ${project.version}. -->
</project>
