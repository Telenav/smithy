package ${package}.${aidLower}.spi.implementation;

/**
 * Dummy class.
 */
public final class Placeholder {
    // This class is simply an empty placeholder so that
    // there is some class in this package for the compiler to
    // find, so the first build after project generation does
    // not fail on the wildcard import in the launchers,
    // which is useful to clarify what one is supposed to do.
    
    // It can be deleted as soon as there is real content to
    // put here.
}